package g03mysimpleirtool.controller;

import java.net.URL;
import java.util.ResourceBundle;
import javafx.fxml.Initializable;

/**
 * La classe {@code G03MySimpleIRToolAboutController} rappresenta il controller
 * MVC della view di informazioni sull'applicazione. Fornisce i metodi handler
 * per la gestione degli eventi di interfaccia grafica.
 */
public class G03MySimpleIRToolAboutController implements Initializable {

    /**
     * Inizializza il controller.
     *
     * @param url URL utilizzata per risolvere i percorsi relativi dell'oggetto
     * radice, o null se la posizione non è nota.
     * @param rb Risorse utilizzate per localizzare l'oggetto radice, o null se
     * l'oggetto radice non è stato localizzato.
     */
    @Override
    public void initialize(URL url, ResourceBundle rb) {

    }

}
